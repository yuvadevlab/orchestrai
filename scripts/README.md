# Scripts

Utility and developer automation scripts for the OrchestrAI monorepo.

- `dev.sh` / `setup.sh`: Local environment bootstrap (Docker containers, DB migrations, model pulls)
- `verify.sh`: Workspace verification, lint, test, and type checking
